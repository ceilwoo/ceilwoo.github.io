<?phpif(!isset($view_type))$view_type = "default";switch($view_type){case "create":echo "您在Tapd项目计划管理系统中有新的迭代。";if(isset($url))echo "详情请点击<a href=".$url.">这里</a>查看。";break;case "update":echo "您在Tapd项目计划管理系统中的迭代被更新了。";$t_change_fields = array();foreach($change_fields as $item){if(isset($this->params['plugin']))$t_change_fields = t($item, "/" . $this->params['plugin']);else$t_change_fields = t($item);}echo "更新的字段为：" . implode("、", $change_fields) . "。";if(isset($url))echo "详情请点击<a href=".$url.">这里</a>查看。";break;default:echo "";break;}?>
